{
  "name": "biologicalsetup",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A canvas-first living specimen workbench for interacting biological systems.",
  "homepage": "https://biologicalsetup.com/",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1",
    "check": "node --check src/random.js && node --check src/render-budget.js && node --check src/morphology.js && node --check src/model.js && node --check src/renderer.js && node --check src/app.js && node --check tools/profile-renderer.mjs",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test",
    "profile:safety": "node tools/profile-renderer.mjs",
    "profile:safety:webkit": "PROFILE_BROWSER=webkit node tools/profile-renderer.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.0"
  }
}
